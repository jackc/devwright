# Initial user defaults only. Subsequent provisioning preserves this file.
default_permissions = "vm_dev"
approval_policy = "on-request"

[shell_environment_policy]
inherit = "all"
ignore_default_excludes = true

[apps._default]
enabled = false
