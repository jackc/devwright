Generated Linux guest verifiers live here (gzip compressed, arm64 and amd64).
Run mise run guest before building or testing the host CLI. These binaries are
embedded in agent-vm and do not need to be shipped separately.
